{
  "name": "smartstay-chuka-backend",
































module.exports = { cloudinary, uploadToCloudinary };};  });    uploadStream.end(fileBuffer);    // Write the buffer to the stream    );      }        }          resolve(result.secure_url);        } else {          reject(error);        if (error) {      (error, result) => {      },        public_id: fileName || undefined        resource_type: 'auto',        folder: 'smartstay-chuka/hostels',      {    const uploadStream = cloudinary.uploader.upload_stream(  return new Promise((resolve, reject) => {const uploadToCloudinary = async (fileBuffer, fileName) => {// Helper function to upload image to Cloudinary});  api_secret: process.env.CLOUDINARY_API_SECRET  api_key: process.env.CLOUDINARY_API_KEY,  cloud_name: process.env.CLOUDINARY_CLOUD_NAME || 'smartstay-chuka',cloudinary.config({// Configure Cloudinary  "version": "1.0.0",
  "description": "Backend API for SMARTSTAY CHUKA hostel booking app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --detectOpenHandles",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": ["hostel", "booking", "chuka", "university"],
  "author": "SMARTSTAY CHUKA",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "sequelize": "^6.35.2",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.5.0",
    "cors": "^2.8.5",
    "express-validator": "^7.0.0",
    "multer": "^1.4.5-lts.1",
    "cloudinary": "^1.40.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
